enum ExpenseType {
  fuel,
  esential,
  food,
  other,
}

enum ButtonType {
  normal,
  outlined,
  text,
  buttonWithIcon,
  backIconButton,
  customButton,
}

enum ImageType {
  generalImage,
  networkImage,
}

enum ExpenseVariant {
  equal,
  percentage,
  money,
}
